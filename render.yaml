services:
  - type: web
    name: phishing-scam-detector
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn phishing_detector.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MODEL_DIR
        value: artifacts
